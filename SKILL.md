---
name: idea-eval
description: 用 5 阶段框架（红线 / 定位 / 价值 / 难度 × 成本 / 框架退场）评估一个开发或开源项目创意。每个阶段的判据都要从最新公开信息中核对，标注来源与核实日期，禁止凭印象下结论。适用于用户提出新点子、问"该不该做 X"、批量筛选候选 idea 的场景。
metadata:
  trigger: 新点子、"该不该做 X"、idea 筛选、批量过滤
  source: https://blog.hancic.site/post/e2150d68-7f24-457d-949b-79485df559d3
---

# Idea Evaluation

把一个点子丢进 5 道关。框架是评审专家，不是决策引擎——砍掉明显不行的，让你在更少的选项里做权衡。

任一关不通过就停并说明理由。5 关都过 → 框架退场，剩下是你自己的 ROI 权衡。

## 支持平台

skill 格式基于通用 Markdown + YAML frontmatter，跨 agent 可移植。已在以下平台验证：

- Claude Code（Anthropic）→ `~/.claude/skills/idea-eval/`
- Codex（OpenAI CLI）→ `~/.codex/skills/idea-eval/`
- Kimi Code（Moonshot）→ `~/.kimi-code/skills/idea-eval/`

一键安装脚本自动探测并装到所有存在的 skills 目录，详见 README.md。

## 何时调用

- 出现新的项目 / 工具 / 库 / 开源点子
- 用户问"该不该做 X？"、"这个值不值得做？"
- 手里有多个候选 idea 要在投入时间前过滤

不要用在：

- 已经是具体决策的点子（"X 怎么实现"）
- 评估已有系统的代码质量
- 单纯技术可行性问题（不带价值 / 成本维度）

## 强制前提：信息核实

**每个阶段在给出结论前，必须先用网络搜索或文档查阅去核实当下事实。** 凭印象或训练数据里的旧知识下结论是这套框架最容易翻车的地方。

具体核实项与推荐源见 [references/framework.md](references/framework.md) 各阶段开头的「先查这些」段。每条结论后必须跟：

- 来源（链接 / 文档名 / API 文档页）
- 核实日期（YYYY-MM-DD）

过期信息标注"时效过期"，并在结论里写明影响。

## 5 个阶段

### 第一关：红线（一票否决）

四类，命中任一即拒，不跑后续阶段。

- **法律风险** — token 中转、盗版资源聚合、刷单刷量
- **道德底线** — 擦边引流、不设限制的 deepfake
- **合规资质** — 荐股、医疗建议、K12 学科辅导等需要资质的领域
- **技术不可控** — 把 AI 放进容错率极低的决策（医疗 / 法律 / 金融）

陷阱：合规这条会动。一开始只是"做信息聚合"，做着做着就越界。判的是轨迹，不是快照。

详细判据与对应法规见 [references/framework.md#第一关红线](references/framework.md)。

### 第二关：定位（决定后续权重）

三选一，每个桶给第三关、第四关分配不同的权重。

- **技术影响力** — 权重：技术深度 + 创新性。商业回报不是首要。成本容忍度高。
- **可商业化变现** — 权重：商业价值 + 成本控制。技术可以为成熟方案妥协。
- **纯粹想做点能被人用的东西** — 权重："有没有人用" + "自己开不开心"。成本容忍度高，无商业目标。

如果点子不在任何桶里，问用户它属于哪一类——否则还不是个真点子。

### 第三关：价值（3 问 + 1 陷阱）

- **真实问题？** 找目标用户里现在"忍着"或"用 Excel 凑合"的人。说"从没想过这个问题" → 大概率伪需求。
- **现有方案够好？** 没有方案，或现有方案太贵 / 太复杂 / 太慢 / 缺关键功能。换皮或加个"AI 自动分类"不算够差。
- **人群够大？** 不需要大众市场，小众够养活独立开发者即可。

**陷阱**："没人做"可能是蓝海，也可能是死海（没需求）。去论坛 / 社群搜抱怨。没有抱怨 → 大概率死海，不是蓝海。

具体怎么搜、搜哪里，见 framework.md。

### 第四关：难度 × 成本（交叉权衡）

不是清单，是对照取舍。

**难度**：

- 核心问题：核心模块能不能从零写出来？
- 不能 → 能不能在项目时间窗内学会？
- 再不能 → 有没有可靠的替代（库 / 服务 / 合作方）？
- 三个都答否 → 难度超预算

每个人有自己的能力边界，跑这一关前先明确。

**成本** — 7 类全看，别只看明显的：

1. 服务器 / 基础设施（持续）
2. 第三方 API 订阅（按量计费，账单上的惊吓项）
3. 一次性（域名 / 证书 / 设计）
4. 维护（bug 修复 / 依赖升级 / 浏览器兼容）
5. 分发（SEO / 内容 / 广告，冷启动时通常 > 技术成本）
6. 冷启动（0 → 第一批种子用户，时间投入最大、即时回报最小）
7. 运营（客服 / 社群 / 用户反馈）

看成本要看与预期收益的比，不是绝对值。月成本 1k、预期收益 1k 不划算。月成本 1w、预期收益 10w 反而值得。

**未知成本陷阱**：爬虫（反爬规则会变）、第三方 API（涨价 / 限流 / 停服）。任一发生都能让项目停摆。

**动手前先画边界**：每个"能不能也加个 X"的需求看着都不大，加着加着就远超最初设计。先定什么不做，比事后砍功能容易得多。

各类成本当前定价怎么查见 framework.md。

### 第五关：框架退场

过完第四关，框架退场。剩下是个人判断——ROI、时间窗口、机会成本——框架帮不了。

## 输出

跑完生成结构化报告。模板见 [references/output-template.md](references/output-template.md)。

每条结论后跟来源链接与核实日期，没查过的标"未核实"，框架判据里写了要查的项没查到 → 不下结论。

报告长度：

- 点子描述一两段：完整报告，约 300 字
- 中等点子（带背景）：完整报告，约 500 字
- 详细点子（用户已写设计稿）：每阶段一段总结，约 200 字 + 引用设计稿里的证据

不灌水，用户是扫读的。

## 多轮使用

框架支持迭代。第一轮可能发现竞品比想象的多（价值分下调）；第二轮可能找到更便宜的替代技术栈（成本降）。每跑一轮对要做的事多一分了解，预期更合理。

用户要求再来一轮时，从第三关重跑。定位与红线基本不变。

## 关联文件

- [references/framework.md](references/framework.md) — 每阶段详细判据 + 推荐核实源
- [references/output-template.md](references/output-template.md) — 报告结构（含来源列）
- [references/examples.md](references/examples.md) — 三个走完整流程的例子（含来源）
- [references/checklist.md](references/checklist.md) — 快速扫描单（含时效核对）